# ReelForge — required packages
PySide6>=6.6
imageio-ffmpeg>=0.4.9
Pillow>=10.0

# Optional — only needed for "Cut on the beat" (beat sync)
# Uncomment and run install_requirements.bat again if you want it:
# librosa>=0.10
